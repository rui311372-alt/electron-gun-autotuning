# 本次本地整理说明

## 复制范围

- `代码/LW` 对应 `code/online`。
- `代码/电子枪离线最优点程序_20260923` 对应 `code/offline`。
- 删减后的 `实验数据` 对应 `data`，保留实验分类及原文件名。
- 不从之前已删除的目录补回文件，不混入其他版本的实验数据。
- 原程序和数据文件采用原样复制，不修改单图 score、聚合规则、CV、实验结果或设备参数。

## 只在本地保留的内容

完整图像、厂商 SDK 及其二进制适配模块、离线历史输出保留在本地副本，由 `.gitignore` 阻止普通提交。它们不是已完成线上发布的内容。后续若选择 Git LFS，应先配置并核查，再调整对应规则；不能直接取消忽略后将整批图像提交。

## 未复制的内容

缓存、编译生成的 Python 字节码、编辑器设置、`.gun_modbus_session`、`SLSDK_PLEORA_log.txt`、临时 Office 锁文件、环境机密配置和私钥类文件不属于本次上传准备范围。原目录中对应文件不删除。

## 后续确认

1. 确认每组实验实际使用的在线脚本及配置；现有多个 `bayes_optlr_*.py` 变体全部保留。
2. 在线依赖清单仍需核对。原 `requirements.txt` 没有列出部分贝叶斯脚本导入的 `skopt` 对应包，本次不擅自改依赖版本。
3. 厂商 SDK 的再分发权限、安装方式和 Python/系统架构兼容性需确认。
4. 原始图像的正式发布方式尚未确定。未提供图像下载入口前，不声称完整图像链路可复现。
5. 原资料中的绝对路径、设备配置及厂商组件应在公开前进一步检查。本次常见凭据检查不等于完整安全审计。
6. `.gitignore` 只控制未跟踪文件，不能替代正式提交前的文件清单核查。
7. 不将目录名称当作数据齐全的证明；空的九工况目录不代表已经提供九工况原始记录。

本次未运行设备程序、安装驱动、调整算法或推送 GitHub。
